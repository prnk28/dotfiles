[user]
	name = Prad Nukala
	email = prad@sonr.io

[credential "https://github.com"]
	helper =
	helper = !/home/prad/.local/share/devbox/global/default/.devbox/nix/profile/default/bin/gh auth git-credential

[credential "https://gist.github.com"]
	helper =
	helper = !/home/prad/.local/share/devbox/global/default/.devbox/nix/profile/default/bin/gh auth git-credential[core]

	editor = nvim
	pager = diffnav

[interactive]
	diffFilter = diffnav

[delta]
	navigate = true  # use n and N to move between diff sections
	dark = true      # or light = true, or omit for auto-detection

[rebase]
	updateRefs = true

[hub]
	protocol = https
	host = sonr-io

[merge]
	conflictstyle = zdiff3

[ghq]
	root = /home/prad/code

[ghq "https://github.com/sonr-io"]
	vcs = git
	root = /home/prad/code/sonr-io

[filter "lfs"]
	process = git-lfs filter-process
	required = true
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f

[net]
	git-fetch-with-cli = true
