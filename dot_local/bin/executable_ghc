#!/bin/bash

# ghc - Persistent Claude Code repository navigator
# Wrapper for gho -C (persistent Claude Code mode)

# Source the gho function
source /home/prad/.local/bin/gho

# Run gho in persistent Claude Code mode
# The -C flag already handles the claude command, no need to pass additional arguments
gho -C